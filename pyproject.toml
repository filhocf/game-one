[project]
name = "game-one"
version = "0.1.0"
description = "Análise estatística de loterias da Caixa (Mega-Sena e Lotofácil)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.5",
]

[project.scripts]
game-one = "game_one.cli:main"
